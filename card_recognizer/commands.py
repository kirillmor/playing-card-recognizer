from __future__ import annotations


def main() -> None:
    """Project command-line entry point."""
    print(
        "Playing Card Recognizer is ready.\n"
        "Use `uv run python -m card_recognizer.training.train` "
        "to inspect the training configuration."
    )
