defaults:
  - data: cards
  - model: baseline_cnn
  - optimizer: adam
  - trainer: cpu
  - logging: mlflow
  - inference: local
  - evaluation: default
  - export: onnx
  - selection: default
  - _self_

project_name: playing-card-recognizer
seed: 42

paths:
  data_dir: data
  artifacts_dir: artifacts
  plots_dir: plots
  reports_dir: reports
