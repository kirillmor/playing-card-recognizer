name: gpu

max_epochs: 25
accelerator: gpu
devices: 1
precision: 16-mixed

log_every_n_steps: 10
check_val_every_n_epoch: 1

early_stopping:
  enabled: true
  monitor: val_macro_f1
  mode: max
  patience: 5

checkpoint:
  monitor: val_macro_f1
  mode: max
  save_top_k: 1
